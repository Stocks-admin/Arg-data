files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      */2 * * * * root curl -X POST https://arg-data.butterstocks.site/update-prices >> /var/log/updatePrices.log 2>&1
commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/mycron.bak"
