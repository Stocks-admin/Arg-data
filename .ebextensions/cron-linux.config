files:
  "/etc/cron.d/mycron":
    mode: "000644"
    owner: root
    group: root
    content: |
      */5 * * * * root curl -X POST https://arg-data.butterstocks.site/update-prices
commands:
  remove_old_cron:
    command: "rm -f /etc/cron.d/mycron.bak"
